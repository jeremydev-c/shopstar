# Resolve Merge Conflict - Commands

After I fix the README.md conflict, run these commands:

```bash
git add README.md
git commit -m "Merge: Resolved README.md conflict, kept local version"
git push -u origin main
```

